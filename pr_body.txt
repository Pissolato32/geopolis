### 🎯 What
Remove the unnecessary `DEFAULT_SURVIVAL_TICKS` export from `src/victory/victoryManager.ts`.

### 💡 Why
Reduce unnecessary module API surface and eliminate dead/unnecessary exported state.

### ✅ Verification
Report:
- repository-wide usage search: Only used in `src/victory/victoryManager.ts`.
- whether the constant was internally used: Yes, it is used internally in `victoryManager.ts`.
- whether any external consumers existed: No external consumers found.
- any affected tests: No tests reference `DEFAULT_SURVIVAL_TICKS`.
- lint result: Passed (`npm run lint`).
- typecheck result: Passed (`npm run typecheck`).
- full test result: Passed (`npm test`).
- build result: Passed (`npm run build`).

### ✨ Result
No gameplay or runtime behavior changes; only the unnecessary export/dead constant is removed.
