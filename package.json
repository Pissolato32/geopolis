{
  "name": "@geopolis/engine",
  "version": "0.1.0",
  "description": "GeoPolis Simulation Engine — scenario-agnostic geopolitical simulation runtime",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm --prefix dashboard run build",
    "seed": "npx tsx src/scripts/seed-modern-world.ts",
    "start": "npm run seed && npx tsx src/index.ts --mode server --host 0.0.0.0 --port 3000",
    "dev": "npm run seed && tsx watch src/index.ts --mode server --host 0.0.0.0 --port 3000",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "private": true,
  "dependencies": {
    "@types/express": "^5.0.6",
    "@types/ws": "^8.18.1",
    "express": "^5.2.1",
    "ws": "^8.21.1"
  }
}
